# Easy-Web AI 模块测试报告

> 日期：2026-07-01  
> 分支：`feat/ai-chat-memory`  
> 执行命令：`python -m pytest tests/ai/ -v`

---

## 测试结果

| 指标 | 值 |
|------|-----|
| 通过 | **25/25** |
| 耗时 | 约 1s |
| 状态 | 全部通过 |

---

## 测试覆盖

### test_memory.py（7 项）— 四层记忆

- L1 工作记忆 read/write
- L2 情景 episode 归档与检索
- L3 semantic chunk + selector_registry 权重
- L3 stale 降权
- L4 skill_candidate 晋升计数
- on_case_success 联动写入
- build_context_prompt 多源合并

### test_tools.py（8 项）— Function Calling 框架工具

- validate_test_case 合法/非法
- save_test_case_staging（tmp 目录，不污染 data/）
- suggest_selector DOM grounding / 反幻觉
- get_config / load_test_cases / memory_search

### test_skills.py（4 项）— Skill Registry

- 加载 3 个内置 Skill
- system prompt 合并
- trigger 匹配
- tool names 聚合

### test_agent_loop.py（4 项）— Agent Loop

- mock 生成登录用例完整闭环（validate → staging → memory）
- ToolExecutor 单工具执行
- mock selector 修复流
- mock 文本回复

### test_notify.py（2 项）— 钉钉报告

- markdown 含关键词「通知」「报告」
- 分支名正确嵌入

---

## 目录合规检查

| 检查项 | 状态 |
|--------|------|
| 根目录无测试脚本 | 通过 |
| knowledge.db 位于 `data/ai/` | 通过 |
| staging 位于 `data/ai/staging/` | 通过 |
| 测试位于 `tests/ai/` | 通过 |
| 运行脚本位于 `scripts/ai/` | 通过 |
| 文档位于 `.doc/20260701doc/` | 通过 |
| AI 日志位于 `logs/ai/`（gitignore） | 通过 |

---

## 钉钉通知

测试通过后通过 `ai/notify.py` 推送 Markdown 报告（关键词：通知、报告）。

---

## 快速验证命令

```bash
# 模块测试
python -m pytest tests/ai/ -v

# 含钉钉通知
python scripts/ai/run_module_tests.py

# Chat mock 演示
python -m ai.chat --mock "帮我生成一个登录用例"
```
