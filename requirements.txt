# Easy-Web 自动化框架依赖

# 核心依赖
selenium>=4.10.0
playwright>=1.40.0

# 数据处理
openpyxl>=3.1.0
pyyaml>=6.0

# 测试框架 & 报告 (Master 分支企业版)
pytest>=7.0.0
allure-pytest>=2.13.0

# 日志 (可选，Python内置logging已足够)
# loguru>=0.7.0

# AI 扩展层 (可选，mock 模式可不安装)
openai>=1.0.0
python-dotenv>=1.0.0
